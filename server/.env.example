# LEGACY Express API (server/) — not used in Cloudflare production.
# Production API is Next.js /api on the Worker with D1 + R2.
# Keep this file only for local Mongo/Cloudinary experiments or data export.
#
# Copy:  cp .env.example .env
# Names only — never commit real values.

PORT=5000
CLIENT_URL=
MONGODB_URI=
JWT_SECRET=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
