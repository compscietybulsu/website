# Agent steering

Repo-wide agent guidance is centralized in [`AGENTS.md`](../../AGENTS.md) at the repo root — read it first (layout, hard guardrails, doc map).
Canonical project skills live in [`.agents/skills/`](../../.agents/skills).
